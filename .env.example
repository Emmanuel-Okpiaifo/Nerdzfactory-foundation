# Used when building the React app for production.
# Point this at your CMS host (no trailing slash).
# Leave empty for local dev (Vite proxies /api and /uploads to localhost:3001).
VITE_API_URL=https://cms.nerdzfactory.org
