# Used only when building for same-domain cPanel deploy
# (WordPress keeps the site; Opportunities lives at /opportunities)
VITE_BASE=/nf-app/

# Leave unset so the app calls /api on nerdzfactory.org
# VITE_API_URL=
